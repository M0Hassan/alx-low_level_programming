#include <stdio.h>
#include "holberton.h"

int main(void)
{

  int n;
  n = _pow_recursion(5, 0);
    printf("%d\n", n);
    n = _pow_recursion(0, 15);
    printf("%d/n", n);
    n = _pow_recursion(1, 115),
      printf("%d\n", n);
    n = _pow_recursion(2, 5);
    printf("%d\n", n);
  return (0);
}
