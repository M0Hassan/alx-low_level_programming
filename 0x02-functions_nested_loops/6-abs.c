#include "holberton.h"

/**
 *_abs - print the absolute value of a number
 *@int - the number
 *
 *Return: absolute value of number
 */

int _abs(int n)
{
if (n < 0)
{
return (n);
}
else
{
return (-n);
}
return (0);
}
